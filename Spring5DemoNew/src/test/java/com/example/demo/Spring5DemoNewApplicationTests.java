package com.example.demo;

import org.junit.Test;
/*import static org.junit.Assert.assertEquals;

import java.net.URI;
import java.net.URISyntaxException;
import java.util.List;

import org.junit.Test;

import org.springframework.http.ResponseEntity;

import org.springframework.web.client.RestTemplate;*/
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest()
public class Spring5DemoNewApplicationTests {
	@Test
	public void contextLoads() {
	}
	/*
	Calculator c=new Calculator();
	RestTemplate resttemp=new RestTemplate();

	@Test
	public void testGetEmployee() throws URISyntaxException
	{
		URI uri=new URI("http://localhost:8080/getE");
		ResponseEntity<String> response=resttemp.getForEntity(uri,String.class);
		assertEquals(response.getStatusCodeValue(),200);
		
	}
	

	@Test
	public void testAddMethod()
	{
		assertEquals(5,c.add(3, 2));
	}
*/}
